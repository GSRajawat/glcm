# Fill in your Supabase credentials.
# Do NOT commit the real .env file. Commit only this .env.example.

SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key

# Optional — not used by this app, but present in template
NODE_ENV=production
